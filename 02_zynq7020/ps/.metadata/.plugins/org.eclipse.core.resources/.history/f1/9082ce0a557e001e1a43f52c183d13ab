/* ------------------------------------------------
 * | UART TYPE   BAUD RATE                        |
 * ------------------------------------------------
 *   ps7_uart    115200 (configured by bootrom/bsp)
 */
#include <stdio.h>
#include "platform.h"
#include "xil_printf.h"
#include "private_timer_init.h"
#include "type.h"

void sys_init()
{
	if( timer_init() != XST_SUCCESS ){
		printf("timer init failed !!!\r\n");
	}else{
		printf("timer init successful !!!\r\n");
	}

	//定时器中断初始化
	timer_intr_init();
}

void proc_frame_sync(){
	static u32 index_new = WIN_LEN;
	static u32 index_old = 0;
}

int main()
{
    init_platform();

    print("\r\nHello World\n\r");
    print("Successfully ran Hello World application\r\n");

    sys_init();
    timer_start();
    uint32_t start = timer_gettime();

    printf("%lu\r\n",start);


    uint32_t end = timer_gettime();
    printf("%lu\r\n",end);

    cleanup_platform();
    return 0;
}
